# todo-api

A simple TODO REST API built with Flask.

## Endpoints
- GET /api/todos — list all todos
- POST /api/todos — create a todo
- PATCH /api/todos/:id — update a todo

## Run
```
pip install -r requirements.txt
python app.py
```
